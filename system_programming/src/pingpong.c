#include <unistd.h>		/*fork(), pid_t*/

int main() {
    /*fork and save pid*/
    /*if fork failed*/
    	/*handle failure*/
    /* if child*/
    	/*call child_process*/
    /*if parent*/
    	/*call parent_process */
}
